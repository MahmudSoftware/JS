let z = 500;

for (i = 0; i <= z; i++) {
  console.log(i); // This is show console
}
//When We khow the range we use for loop  otherwise we will use while loop

while (true) {
  let num = Math.ceil(Math.random() * 100);
  console.log(num);

  if (num == 2) {
    break;
  }
}
